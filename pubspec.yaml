name: gsettings
version: 0.2.3

description:
  GSettings client

homepage: https://github.com/canonical/gsettings.dart

environment:
  sdk: '>=2.12.0 <3.0.0'

platforms:
  linux:

dependencies:
  dbus: ^0.6.0
  xdg_directories: ^0.2.0

dev_dependencies:
  lints: ^1.0.1
  test: ^1.16.8
  test_cov: ^1.0.1
